import 'package:flutter/material.dart';

class OrderBodyScreen extends StatelessWidget {
  const OrderBodyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Order Screen'));
  }
}